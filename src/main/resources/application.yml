server:
  port: 8080
  address: 0.0.0.0

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        ddl-auto: true
    defer-datasource-initialization: true

  datasource:
    url: jdbc:mysql://localhost:3306/univDB
    username: root
    password: jinwook219

  # 이거 추가
  web:
    resources:
      static-locations: classpath:/static/
  mvc:
    static-path-pattern: /static/**

springdoc:
  swagger-ui:
    path: /swagger-ui.html